/*
package com.example.roomdb

import kotlinx.coroutines.*



fun main() = runBlocking { // this: CoroutineScope
    println("run blocking thread is ${Thread.currentThread().name}")
    GlobalScope.async { // launch new coroutine in the scope of runBlocking
        println("launch thread is ${Thread.currentThread().name}")
        delay(1000L)
        println("World!")
    }
    println("Hello,")
}
*/
